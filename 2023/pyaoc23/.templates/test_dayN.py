from unittest import TestCase
from days.day{day} import partA, partB

class TestDay{day}(TestCase):
    example="""example"""
    def testPartA(self):
        res = partA(self.example)
        self.assertEqual(res, 0)

    def testPartB(self):
        res = partB(self.example)
        self.assertEqual(res, 0)